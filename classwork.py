# matt, kelsy, adley, josh, jake

names = ['matt', 'kelsy', 'adley', 'josh', 'jake']
names.sort()
first_name = names[0]

# [:-1] gives everything up to the last character of the string
first_name = first_name[:-1]
print(first_name)

# just loop through and get the longest here